# commands.json の書き方

`commands.json` に、今ターン誰が何をするかを書きます。書いたら `node sim/step.ts` を実行してください。

**書かなかった人は、自動でいつもの行動（常設命令）が実行されます。** 全員分書く必要はありません。

---

## 基本の形

外側は `[` と `]` で囲み、1人分ずつ `{ ... }` を書いて、間を `,`（カンマ）で区切ります。
**最後の1個のあとには `,` を付けないでください。** よくある間違いです。

```json
[
  { "type": "建設", "player": "p01" },
  { "type": "開拓", "player": "p02" }
]
```

---

## 行動の種類（9つ）。下からコピーして書き換えてください。

### 建設する（都市を発展させる。資源が足りないと失敗する）

```json
{ "type": "建設", "player": "p01" }
```

### 開拓する（土地を広げる）

```json
{ "type": "開拓", "player": "p01" }
```

### 取引を持ちかける（提案する）

```json
{ "type": "提案", "player": "p01", "to": "p05", "give": "資材", "giveAmount": 9, "take": "知識", "takeAmount": 9, "turns": 8 }
```

- `player` … 提案する人
- `to` … 相手
- `give` / `giveAmount` … 自分が渡す資源と数（食料・資材・知識のどれか）
- `take` / `takeAmount` … 代わりにもらう資源と数
- `turns` … 何ターン続けるか

### 取引を受け入れる（承諾する）

```json
{ "type": "承諾", "player": "p01", "contractId": "C1-p05-3" }
```

`contractId` は、直前に実行したときの表示や `out/state.json` の中の契約一覧に書いてあります。

### 取引をやめる（破棄する）

```json
{ "type": "破棄", "player": "p01", "contractId": "C1-p05-3" }
```

信用が下がるので注意してください。

### 土地を譲る提案をする（合意制。取引と同じく手番を消費しない）

```json
{ "type": "土地提案", "player": "p01", "to": "p05", "x": 12, "y": 7, "wantResource": "知識", "wantAmount": 30 }
```

- `x` / `y` … 自分が持っているマスの座標（`out/map.svg` や `out/state.json` で確認できます）
- `wantResource` / `wantAmount` … 代わりにもらいたい資源と数

相手が承諾した瞬間に、そのマスは相手のものになり、資源が支払われます。一度きりの取引で、資源の取引のように毎ターン続くものではありません。

### 土地の提案を受け入れる（土地承諾）

```json
{ "type": "土地承諾", "player": "p05", "landOfferId": "L1-p01-0" }
```

`landOfferId` は `out/state.json` の中の `landOffers` 一覧、または実行結果の表示で確認できます。承諾した時点で、あなたの持っている資源が足りないと不成立になります。

### 信用の低い隣人から土地を奪う（奪う）

```json
{ "type": "奪う", "player": "p01", "x": 12, "y": 7 }
```

**信用がかなり低い人（相手の同意なし）** の、自分の領土に隣接しているマスだけを対象にできます。開拓よりコストが高く、都市が建っているマスは奪えません。本番の行動を1つ消費します（建設・開拓と同じ枠）。

### 何もしない（待機する）

```json
{ "type": "待機", "player": "p01" }
```

---

## 複数人分をまとめて書く例

```json
[
  { "type": "建設", "player": "p01" },
  { "type": "提案", "player": "p02", "to": "p05", "give": "資材", "giveAmount": 9, "take": "知識", "takeAmount": 9, "turns": 8 },
  { "type": "承諾", "player": "p05", "contractId": "C1-p02-1" }
]
```

---

## うまくいかないときは

`node sim/step.ts` を実行して、赤いエラーが出た場合は**世界はまだ進んでいません**。エラー文の指示に従って `commands.json` を直してから、もう一度実行してください。
